# Korean default + original audio — 2026-09-22

## Scope and implementation

Continue existing Web-first project from 3cba608. No native work, browser access,
host configuration changes or public deployment.

- Godot built-in gettext PO catalogs (Korean + English), Korean default, bundled
  Noto Sans KR (SIL OFL 1.1). UI, catalog names/descriptions, equipment stats,
  stages, HUD, blessings, victory/defeat, save errors, settings and orientation
  notice are localized at display time. Internal IDs and save v1 unchanged.
- Full unit/skill names are retained instead of taking the last English word.
  Ten equipment descriptions added, with no numeric balance changes.
- Web shell/failure/offline messages and PWA metadata also default to Korean.
- Direct original synthesis: 2 BGM (menu/battle) and 8 SFX (click, summon, impact,
  ranged, skill, heal, victory, defeat), with provenance in assets/audio.
- Scene-based BGM switching, first-gesture audio start, looping streams, actual
  damage/projectile/healing hooks. Existing Master/Music/SFX and mute settings
  now use explicit STREAM playback on Web and true bus mute at zero volume.
- Aura, pool acceptance/damage/cleanup, rear support AI, rollback and input
  semantics are preserved.
- Web export includes font OFL license and excludes prior generated build/review
  resources. Android/iOS preset sections were not changed.

## Direct verification (not inherited test claims)

Godot 4.6.stable.official.89cea1439 macOS headless:

- tests/presentation_test.gd: **154 checks / 0 failures**. Korean default and all
  catalog name/description fields, English translation switch, Hangul font
  glyph availability, Korean menu/equipment/settings/stage/HUD/result/pause/
  reward retry, actual audio events, BGM routing/loops, bus volume/zero/mute,
  1280px minimum-width layout checks. Korean HUD minimum width: 1239px.
- tests/test_runner.gd: **84 checks / 0 failures** (11 focused integration
  cases, including campaign simulation; not automatic-physics E2E).
- tests/audit_suite.gd: **10 cases / 115 assertions / 0 failures**, including
  aura, pool, damage/target edges, save rollback, support rear positioning.
- tests/runtime_e2e.gd: **28 assertions / 0 failures** for real Main Scene
  automatic-physics Stage 1/2 + menu/training/equipment/settings interaction;
  fresh-process reload: **4 assertions / 0 failures**. Reload is the continuation
  of the same case, not another gameplay case.
- Existing suite subtotal: **231 assertions**, not the historical 237-assertion
  full-suite result. Separate save_probe and boss-only runtime were not rerun.
- Legacy text-selector tests explicitly select English. Dedicated presentation
  tests exercise Korean default, so neither language masks the other.
- node --test tools/web-release.test.mjs: **7/7** passed; includes new Korean
  shell/offline/manifest case and existing versioning/cache/isolation tests.
- Final Godot Web release export: exit 0; no SCRIPT ERROR or ERROR diagnostics.
  Engine prints its embedded ICU informational notice and the existing
  shutdown ADB-daemon diagnostic; no native build or SDK setup was performed.
- Final local release: **5c1f6b23b9fdfbdc**, build/web.
- Local HTTP verification with existing tools/serve.mjs:
  **16/16** files return HTTP 200 and exactly match disk bytes; WASM MIME,
  Korean shell/manifest/offline, translation/audio/font/license inclusion and
  exclusion of previous generated build content checked.
  Fingerprints: web_ko_audio_2026-09-22.json. Combined checked bytes: 50,318,529.
- git diff --check passed before commit.

## Test isolation and evidence

QA runs used .tools/presentation-qa, a project-local copy of current source.
Only its project.godot custom user directory was changed to "userdata"; on
macOS, HOME was omitted in the **test subprocess environment only**, making
Godot's documented implementation use its current directory for data storage.
No host environment setting was changed. All test saves/logs stayed under that
project-local QA directory. Import used the existing self-contained editor.

Local logs (Git-ignored):
.tools/presentation-qa/artifacts/qa-{0..5}.log and suite JSONs;
artifacts/export-web-ko-audio.log. Initial development failures (incorrect nested
translation call, test-script preload ordering, English-only legacy selectors)
were corrected before the final passes above. The isolated editor's initial
snapshot-directory error was fixed by separating editor import from the isolated
game runtime environment. A supplemental inline Node PCM probe was blocked by
exec approval policy and was not run; no waveform/clipping result is claimed.

## Delivery boundary / remaining verification

Source and source audio/font resources are committed; build/web is local and
Git-ignored as before. **gh-pages was not changed by this work unit**.
Public URL remains https://kkzzxxcc.github.io/lantern-march/ on previously
deployed release 0d67ff167e3ab7dd until a separate deployment.

No browser startup, mobile screenshot, audible listening/autoplay lifecycle,
PWA update or IndexedDB restart-persistence result is claimed. Mobile listening,
real-device typography and new-release deployment remain next steps.
